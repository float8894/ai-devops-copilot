// it('should connect to postgres and return seeded jobs', async () => {
//   const rows = await query('SELECT count(*) FROM jobs WHERE status = $1', [
//     'failed',
//   ]);
//   expect(Number(rows[0]?.count)).toBeGreaterThan(0);
// });
